import React from 'react';
import { ScrollView, Text } from 'react-native';

/**
 * Connected apps settings in the settings overlay.
 */
function ConnectedAppsPane() {
  return (
    <ScrollView>
      <Text>Connected apps settings</Text>
    </ScrollView>
  );
}

export default ConnectedAppsPane;
